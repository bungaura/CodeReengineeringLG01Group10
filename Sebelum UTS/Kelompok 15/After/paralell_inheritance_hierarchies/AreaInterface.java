package paralell_inheritance_hierarchies.before;

public class AreaInterface {
	public static float areaOfTriangle(Triangle t) {
		return t.getHeight() * t.getWidth() / 2;
	}

	public static float areaOfRectangle(Rectangle t) {
		return r.getWidth() * r.getHeight();
	}
}
